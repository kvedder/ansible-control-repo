all:
  children:
    production:
      hosts:
        bacula:
          ansible_host: x.x.x.x
          ansible_user: ansible

      vars:
        ansible_ssh_private_key_file: /home/semaphore/ssh_key


